## Introduction
In the `image_backbones` folder, the files that have already been converted into packages<sup>1</sup> are functional image models that have been validated through code. They also already come with ready-to-use mem adaptation.
## More Details
<sup>1</sup> Package: a folder containing an `__init__.py` file.
